import styled from 'styled-components';
import colors from 'components/colors';

const Styles = {
  NoResults: styled.h2`
    color: ${colors.red};
    padding: 0.25rem;
    text-align: center;
  `,
};

export default Styles;
