import styled from 'styled-components';
import { Tab } from 'rebass';

const Styles = {
  Tab: styled(Tab)`
    cursor: pointer;
  `,
  Strong: styled.span`
    font-weight: 700;
  `,
};

export default Styles;
